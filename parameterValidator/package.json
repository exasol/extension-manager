{
  "scripts": {
    "build": "npm run clean && npm run compile",
    "compile": "tsc --build && esbuild dist/*.js --bundle --outfile=dist.js --target=es6",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@exasol/extension-parameter-validator": "0.2.0"
  },
  "devDependencies": {
    "esbuild": "0.14.54",
    "typescript": "4.7.4"
  }
}
