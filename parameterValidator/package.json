{
  "dependencies": {
    "@exasol/extension-parameter-validator": "0.1.1"
  },
  "devDependencies": {
    "esbuild": "0.14.37",
    "typescript": "4.6.3"
  },
  "scripts": {
    "build": "tsc --build && esbuild dist/*.js --bundle --outfile=dist.js --target=es6",
    "clean": "tsc --build --clean"
  }
}
