{
  "scripts": {
    "build": "tsc --build && esbuild dist/extensionForTesting.js --bundle --outfile=dist.js --target=es6",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@exasol/extension-manager-interface": "0.1.14"
  },
  "devDependencies": {
    "esbuild": "0.15.7",
    "typescript": "4.8.3"
  }
}
