{
  "scripts": {
    "build": "tsc --build && esbuild dist/backend/integrationTesting/extensionForTesting/extensionForTesting.js --bundle --outfile=dist.js --target=es5",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "typescript": "4.6.3",
    "esbuild": "0.14.37"
  }
}
