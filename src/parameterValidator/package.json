{
  "scripts": {
    "build": "npm run clean && npm run compile",
    "compile": "tsc --build && esbuild dist/*.js --bundle --outfile=parameterValidator.js --target=es6",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@exasol/extension-parameter-validator": "0.2.0"
  },
  "devDependencies": {
    "esbuild": "0.15.7",
    "typescript": "4.8.3"
  }
}
