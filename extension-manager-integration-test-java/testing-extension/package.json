{
    "name": "testing-extension",
    "version": "0.0.0",
    "main": "dist/extension.js",
    "description": "Extension for testing Extension Manager test setup",
    "license": "MIT",
    "scripts": {
        "build": "tsc --build && esbuild dist/extension.js --bundle --outfile=dist/s3-vs-extension.js --target=es6",
        "clean": "rm -rf dist/"
    },
    "dependencies": {
        "@exasol/extension-manager-interface": "0.1.15"
    },
    "devDependencies": {
        "esbuild": "^0.14.50",
        "typescript": "^4.7.4"
    }
}